/**
 * @fileoverview Brute-force baseline produzido em `@rinha26/core`. Re-exportado aqui
 * para que `apps/api` faça `import { BruteForceVectorIndex } from '@rinha26/vector-store'`
 * sem depender de adapters embutidos do core.
 *
 * Variações otimizadas (typed arrays, SIMD, pré-cálculos) entram em arquivos próprios
 * neste mesmo diretório (ex.: `brute-force-typed.ts`).
 */

export { BruteForceVectorIndex } from '@rinha26/core';
